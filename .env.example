# OpenAI API キー
# Mem0 のコア機能（埋め込み生成や LLM 推論）に使用されます。
# 必須項目です。
OPENAI_API_KEY=sk-placeholder-please-replace-me

# ユーザー ID
# メモリを関連付けるユーザーの識別子です。
# 任意の文字列を指定できます（例: default-user, my-agent-01）。
USER=default-user

# API の URL
# フロントエンド (UI) がバックエンド (MCP Server) に接続するための URL です。
# Docker Compose 内での通信ではなく、ブラウザからのアクセス用なので localhost を指定します。
NEXT_PUBLIC_API_URL=http://localhost:8765

# フロントエンド用ユーザー ID
# UI で表示・操作する対象のユーザー ID です。
# 上記の USER 変数と同じ値を設定してください。
NEXT_PUBLIC_USER_ID=default-user
